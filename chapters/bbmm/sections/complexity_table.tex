\begin{table*}[t!]
  \caption{
    Asymptotic complexities of matrix-vector multiplications (MVMs),
    computing the pivoted-Cholesky preconditioner (PC-precond.),
    and performing inference.
    ($n$ data points, $m$ inducing points, $p$ iterations of CG, and $t$ initial probe vectors for Lanczos.)
    \jrg{My vote is to remove the preconditioner from the running times -- the complexities are just too complicated imo}
    \label{tab:running_times}
  }
  \vspace{0.5ex}
  \centering
  \resizebox{\textwidth}{!}{%
    \begin{tabular}{ |c||c|c||c|c||c| }
      \hline
      \
      \multirow{2}*{\bf Method}
      & \multirow{2}*{\bf One MVM}
      & {\bf PC}
      & {\bf MVM-based inference}
      & {\bf MVM-based inference}
      & \multirow{2}*{\bf Cholesky-based inference}
      \\
      &
      & {\bf precond.}
      & (no precond.)
      & (with PC-precond.)
      &
      \\
      \hhline{|=#=|=#=|=#=|}
      Standard GP
      & $\bigo{n^2}$
      & $\bigo{n k^2}$
      & $\bigo{t p (n^2)}$
      & $\bigo{t p (n^2) + t n k^2}$
      & $\bigo{n^3}$
      \\
      SGPR
      & $\bigo{n m + m^2}$
      & $\bigo{n m k^2}$
      & $\bigo{t p (n m + m^2)}$
      & $\bigo{t p (n m + m^2) + n m k^2}$
      & $\bigo{n m^2 + m^3}$
      \\
      SKI
      & $\bigo{n + m \log m}$
      & $\bigo{n k^2}$
      & $\bigo{t p (n + m \log m)}$
      & $\bigo{t p (n + m \log m) + t n k^2}$
      & --
      \\
      \hline
    \end{tabular}
  }
  \vspace{-2ex}
\end{table*}
